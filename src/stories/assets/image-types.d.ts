// Types for the static image imports in stories (`import img from './x.jpg'`).
// `next-env.d.ts` carries the same reference but is gitignored and only exists
// after `next dev` / `next build`, so CI's type-check would not see it.
/// <reference types="next/image-types/global" />
